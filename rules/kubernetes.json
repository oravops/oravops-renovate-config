{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Limit Kubernetes versions to avoid breaking changes.",
  "packageRules": [
    {
      "extends": ["github>oravops/oravops-renovate-config//packages/kubernetes#main"],
      "allowedVersions": "< 1.34.0"
    }
  ]
}
